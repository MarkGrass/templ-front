import React, { FC } from 'react';

import { AppPropTypes } from './app.types';
import './app.css';

export const App: FC<AppPropTypes> = () => {
    return <div className="app" />;
};

export default App;
